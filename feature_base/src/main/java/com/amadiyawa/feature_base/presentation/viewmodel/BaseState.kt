package com.amadiyawa.feature_base.presentation.viewmodel

/**
 * The BaseState interface is a marker interface used to tag classes that represent a state in your application.
 *
 * It doesn't enforce any structure or behavior on the classes that implement it, but it provides a way to treat those classes in a uniform manner in your code.
 */
interface BaseState